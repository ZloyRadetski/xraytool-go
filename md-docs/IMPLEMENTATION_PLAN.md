# План рефакторинга xraytool-go в API-First Бэкенд

Данный план описывает пошаговую трансформацию утилиты `xraytool-go` в полноценный монолит с REST API и реляционной базой данных, строго следуя принципам из `architecture_plan.md`.

## Подготовительный этап
- [x] **Шаг 0.1:** `gorm.io/gorm v1.31.1`, `gorm.io/driver/postgres v1.6.0`, `github.com/glebarez/sqlite v1.11.0` — добавлены. `go mod tidy` чисто. ✅
- [x] **Шаг 0.2:** Структура `internal/database`, `internal/server`, `internal/events` — создана. ✅

## Этап 1: База Данных (Модели и Подключение)
*Задача: Отказаться от текстовых файлов в пользу GORM.*
- [x] **Шаг 1.1:** `internal/database/models.go` — `User`, `Subscription`, `Device`, `Payment`, `ReferralReward` с JSONB `Metadata`. ✅
- [x] **Шаг 1.2:** `internal/database/db.go` + `driver_sqlite.go` + `driver_postgres.go` — синглтон, `AutoMigrate`, переключение между драйверами. ✅

## Этап 2: Система Событий (Event Engine)
*Задача: Сделать так, чтобы бот и сайт могли мгновенно узнавать об изменениях (например, об успешной оплате).*
- [x] **Шаг 2.1:** Создать `internal/events/dispatcher.go`. ✅ (файл уже существовал с более продвинутой реализацией)
- [x] **Шаг 2.2:** Описать структуру стандартного JSON-события. ✅
- [x] **Шаг 2.3:** `Dispatch()` (async) + `DispatchSync()` + retry 4 попытки + контекст. ✅

## Этап 3: Ядро REST API (Маршрутизация)
*Задача: Подготовить фундамент для обработки HTTP-запросов.*
- [x] **Шаг 3.1:** Создать `internal/server/router.go`. ✅
- [x] **Шаг 3.2:** `http.ServeMux` с method+path паттернами Go 1.22+. ✅ 20+ маршрутов зарегистрированы.
- [x] **Шаг 3.3:** `authMiddleware` для проверки `X-API-Key`. ✅

## Этап 4: Разработка Контроллеров (Бизнес-логика)
*Задача: Перенести логику управления данными в Go.*
- [x] **Шаг 4.1:** Создать `internal/server/handlers_user.go`.
  - Эндпоинт регистрации пользователя `POST /api/v1/users/register`.
  - Эндпоинт получения данных пользователя `GET /api/v1/users/telegram/{tg_id}`.
  - Админские эндпоинты (блокировка, ручное продление).
- [x] **Шаг 4.2:** Создать `internal/server/handlers_payment.go`.
  - Эндпоинт создания платежа `POST /api/v1/payments/create`.
  - Эндпоинт коллбэка от платежки (например, Platega), который обновляет баланс и вызывает `events.Publish()`.
- [x] **Шаг 4.3:** Создать `internal/server/handlers_sub.go`.
  - Перенести сюда логику выдачи конфигов Xray (нынешний `/client` и `/api/v1/sub`).
  - Связать выдачу конфига с таблицами `subscriptions` и `devices` (проверка HWID и лимита устройств).

## Этап 5: Склейка и Интеграция с `cmd/server.go`
*Задача: Запустить новый API внутри существующей команды.*
- [x] **Шаг 5.1:** Отредактировать существующий файл `cmd/server.go`.
- [x] **Шаг 5.2:** Добавить инициализацию БД (`database.Init()`) при старте сервера.
- [x] **Шаг 5.3:** Заменить старые костыли маршрутизации (где `exec.Command` дергал бинарник) на вызов нашего нового роутера из `internal/server.NewRouter()`.

## Этап 6: Инструмент Миграции (Перенос легаси-данных)
*Задача: Бесшовно перенести пользователей из старого бота в новую БД.*
- [x] **Шаг 6.1:** Создать файл `cmd/migrate_db.go`.
- [x] **Шаг 6.2:** Написать код, который читает старый файл `Clients/bot.db` (используя нативный драйвер SQLite).
- [x] **Шаг 6.3:** Написать логику мэппинга (перевода старых колонок SQLite в новые структуры GORM) и сделать пакетный `INSERT` в PostgreSQL/SQLite, плюс перенести JSON устройства.
- [x] **Шаг 6.4:** Зарегистрировать новую команду `xraytool db-migrate` в `cmd/root.go`.
